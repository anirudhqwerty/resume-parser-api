# FastAPI and Server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Pydantic (Core dependency - must be compatible with FastAPI)
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic-core==2.23.4

# Environment Variables
python-dotenv==1.0.1

# Database - Supabase Stack (Critical: These versions MUST match)
supabase==2.7.4
postgrest==0.17.1
storage3==0.8.1
supafunc==0.5.1
gotrue==2.8.1
realtime==2.0.5

# MongoDB
pymongo==4.8.0
dnspython==2.6.1  # Required by pymongo for SRV connections

# HTTP Client (CRITICAL: Must be compatible with supabase)
httpx==0.27.0
httpcore==1.0.5
h11==0.14.0
certifi==2024.8.30

# File Processing
PyMuPDF==1.24.10
python-docx==1.1.2
Pillow==10.4.0  # Required by PyMuPDF

# HTTP Requests (for HuggingFace/Groq API)
requests==2.32.3
urllib3==2.2.2
charset-normalizer==3.3.2
idna==3.8

# Type Hints and Annotations
typing-extensions==4.12.2
annotated-types==0.7.0

# Async Support
anyio==4.6.0
sniffio==1.3.1

# Additional Core Dependencies
click==8.1.7  # Required by uvicorn
starlette==0.38.6  # FastAPI dependency - must match FastAPI version

# Security and Encoding
python-jose==3.3.0  # JWT tokens (if using auth)
passlib==1.7.4  # Password hashing (if needed)
bcrypt==4.2.0  # Secure hashing

# Logging (Optional but recommended)
python-json-logger==2.0.7

# Date/Time handling
python-dateutil==2.9.0